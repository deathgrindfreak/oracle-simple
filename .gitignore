.stack-work/
.stack-root/
*~
.env
dist-newstyle

TAGS

# hie files are generated by ghc for use by tooling
# our scripts should cover not dumping these outside of .stack-work/
# but just in case they are ignored here as well
*.hie
.DS_Store

# Files copied into containers
containers/oracle-buildpack/functional-tests
